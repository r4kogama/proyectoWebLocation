import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProfileSecurityFormComponent } from './profile-security-form.component';

/* describe('ProfileSecurityFormComponent', () => {
  let component: ProfileSecurityFormComponent;
  let fixture: ComponentFixture<ProfileSecurityFormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProfileSecurityFormComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ProfileSecurityFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
